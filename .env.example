# Copy to .env if the M-Pesa backend isn't running on the default http://localhost:4000
VITE_MPESA_API_BASE=http://localhost:4000

# Copy to .env if the Flask backend (Backend/) isn't running on the default http://localhost:5000
VITE_API_BASE=http://localhost:5000
